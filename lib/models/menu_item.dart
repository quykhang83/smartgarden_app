import 'package:flutter/material.dart';

class MyMenuItem {
  final String title;
  final IconData icon;

  // MyMenuItem({required this.title, required this.icon});
  const MyMenuItem(this.title, this.icon);
}